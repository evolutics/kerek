Vagrant.configure("2") do |configuration|
  configuration.vm.box = "ubuntu/focal64"
  configuration.vm.network "private_network", ip: ENV["KEREK_IP"]
  configuration.vm.synced_folder ".", "/vagrant", disabled: true
  configuration.vm.provider "virtualbox" do |virtualbox|
    virtualbox.memory = "2048"
  end
end
